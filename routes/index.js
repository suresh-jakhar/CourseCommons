const userRoutes = require("./user/router");
const courseRoutes = require("./course/router");
const instructorRoutes = require("./instructor/router");

module.exports = {
    userRoutes,
    courseRoutes,
    instructorRoutes
};
